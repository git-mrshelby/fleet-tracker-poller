services:
  - type: worker
    name: fleet-tracker
    runtime: python
    plan: free
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python fleet_supabase_pusher.py --interval 1
